apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-apps-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: todo-apps-svc
                port:
                  number: 2345
