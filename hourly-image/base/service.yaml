apiVersion: v1
kind: Service
metadata:
  name: hourly-image-svc
  annotations:
    cloud.google.com/neg: '{"exposed_ports": {"80":{}}}'
spec:
  type: ClusterIP
  selector:
    app: hourly-image # This is the app as declared in the deployment.
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
